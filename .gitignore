# Node modules and temporary build artifacts
node_modules/
# Pasta node_modules dentro de functions
functions/node_modules/
# Saída de compilação da função (JavaScript compilado)
functions/lib/

# Logs and debug files
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
firebase-debug.log
firebase-debug.*.log

# Environment variables and local configs (CRITICAL for security)
.env
.env.local
*.env.local
.firebase/         # Configurações de cache do Firebase CLI

# Build outputs (geralmente para frontend)
dist/
build/
out/
.next/
dist-ssr/

# Editor and OS files
.vscode/           # Configurações específicas do VS Code (remova !.vscode/extensions.json se você quiser compartilhar)
!.vscode/extensions.json # Manter se você quer que o Git rastreie sua lista de extensões recomendadas do VS Code
.idea/             # Configurações do WebStorm/IntelliJ
.DS_Store          # Arquivos de sistema do macOS
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test/coverage reports
coverage/
.nyc_output/

# Common temporary files
*.tmp
*.bak
*.swo
*.swp

# Ignorar o arquivo '{' de 0KB (se ainda existir)
{

# Ignore files generated by npm/yarn (already covered by node_modules but good to be explicit)
package-lock.json
yarn.lock
pnpm-lock.yaml

# If your functions folder also has a .gitignore, make sure it's consistent
# but usually, having one comprehensive .gitignore at the root is enough.